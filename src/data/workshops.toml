# Ordered list of workshop IDs.
# Each ID must match a filename in src/data/workshops/<id>.toml.
# If `order` is empty or omitted, workshops are sorted by file order.
order = ["software-design", "practical-ai", "tinygo", "tinygo-keeb"]

# Workshop instructors:
# - For conference speakers: use `speakers = ["speaker-id"]` (references src/data/speakers/)
# - For external/community instructors: use `[instructor]` table with name, url, image, bio, socialUrl

